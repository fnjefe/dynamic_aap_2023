---
ansible-navigator:
  ansible:
    config:
      path: ./ansible.cfg
  execution-environment:
    image: ee-supported-rhel8
    pull:
      policy: missing

#  mode: stdout

  playbook-artifact:
    enable: false

